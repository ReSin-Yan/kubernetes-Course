apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: demo-pvc
  labels:
    app: demo
    pvc: demo
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteM
  resources:
    requests:
      storage: 1Gi
